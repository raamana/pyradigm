language: python
cache: pip
python:
  - "3.5"
  - "3.6"

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: pytest pyradigm/tests/test_pyradigm.py
